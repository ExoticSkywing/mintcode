services:
  api:
    build: .
    command: ["uvicorn", "mintcode_api.app:app", "--host", "0.0.0.0", "--port", "8123"]
    environment:
      ADMIN_API_KEY: ${ADMIN_API_KEY:-change-me}
      VOUCHER_SECRET: ${VOUCHER_SECRET:-change-me-too}
      DATABASE_URL: ${DATABASE_URL}
      DEV_AUTH_TIMESTAMP_SKEW_SECONDS: ${DEV_AUTH_TIMESTAMP_SKEW_SECONDS:-300}
      DEV_RATE_LIMIT_VOUCHER_PER_MIN: ${DEV_RATE_LIMIT_VOUCHER_PER_MIN:-30}
      DEV_RATE_LIMIT_DEV_KEY_PER_MIN: ${DEV_RATE_LIMIT_DEV_KEY_PER_MIN:-60}
      DEV_RATE_LIMIT_IP_PER_MIN: ${DEV_RATE_LIMIT_IP_PER_MIN:-120}
      DB_AUTO_CREATE_TABLES: ${DB_AUTO_CREATE_TABLES:-true}
      WORKER_LEASE_SECONDS: ${WORKER_LEASE_SECONDS:-30}
      WORKER_ID: ${WORKER_ID:-}
      BUY_INFLIGHT_SECONDS: ${BUY_INFLIGHT_SECONDS:-20}
      MAX_BUY_ATTEMPTS: ${MAX_BUY_ATTEMPTS:-3}
      REDEEM_PROCESS_MODE: external
      REDEEM_WAIT_SECONDS: ${REDEEM_WAIT_SECONDS:-120}
      FIVESIM_API_KEY: ${FIVESIM_API_KEY:-}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "8123:8123"

  worker:
    build: .
    command: ["python", "-m", "mintcode_api.worker"]
    environment:
      ADMIN_API_KEY: ${ADMIN_API_KEY:-change-me}
      VOUCHER_SECRET: ${VOUCHER_SECRET:-change-me-too}
      DATABASE_URL: ${DATABASE_URL}
      DB_AUTO_CREATE_TABLES: ${DB_AUTO_CREATE_TABLES:-true}
      WORKER_LEASE_SECONDS: ${WORKER_LEASE_SECONDS:-30}
      WORKER_ID: ${WORKER_ID:-}
      BUY_INFLIGHT_SECONDS: ${BUY_INFLIGHT_SECONDS:-20}
      MAX_BUY_ATTEMPTS: ${MAX_BUY_ATTEMPTS:-3}
      REDEEM_PROCESS_MODE: external
      REDEEM_WAIT_SECONDS: ${REDEEM_WAIT_SECONDS:-120}
      FIVESIM_API_KEY: ${FIVESIM_API_KEY:-}
    extra_hosts:
      - "host.docker.internal:host-gateway"
